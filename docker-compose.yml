name: actum

services:
  # ── Main robot agent service (headless or web server) ──────────────────────
  actum:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        CUDA_VERSION: "12.4.1"
        UBUNTU_VERSION: "22.04"
    
    image: actum:latest
    container_name: actum-agent
    
    # GPU support (requires nvidia-docker or Docker 20.10+)
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    
    # API keys (OPENAI_API_KEY, …) loaded from the project .env if present.
    env_file:
      - path: .env
        required: false

    # Environment variables
    environment:
      - PORT=8000
      - MODEL_PATH=/root/.cache/huggingface/models--litert-community--gemma-4-E2B-it-litert-lm/snapshots/*/gemma-4-E2B-it.litertlm
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
      # Audio device (adjust if needed: hw:1,0 for different card)
      - ACTUM_AUDIO_DEVICE=hw:2,0
      # Camera mode: auto, csi, usb, or device index
      - ACTUM_CAMERA=usb
    
    # Grant device cgroup permissions for speaker/camera
    devices:
      - "/dev/snd:/dev/snd"
      - "/dev/video0:/dev/video0"
      - "/dev/video1:/dev/video1"

    # Volumes for persistence + hardware access
    volumes:
      # Config file (host config.json → container)
      - ./config.json:/workspace/config.json
      
      # Model cache (persistent across runs)
      - actum-huggingface:/root/.cache/huggingface
      
      # Agent memory + logs
      - ./logs:/workspace/logs
      
      # Hardware access (if robot is on same host)
      # Uncomment to enable hardware bridge
      # - /dev/ttyUSB0:/dev/ttyUSB0  # Serial ports (adjust as needed)
      - /dev/snd:/dev/snd            # Audio devices
      - /dev/video0:/dev/video0      # Camera device (USB or CSI)
    
    # Ports: in network_mode: host, ports mapping is not needed as the container
    # shares the host network namespace.
    # ports:
    #   - "8000:8000"  # Web dashboard (headless mode: not used)
    
    # Allow container to access host network (essential for CycloneDDS multicast communication)
    network_mode: host
    
    # Interactive terminal (for text input)
    stdin_open: true
    tty: true
    
    # Health check
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

# Named volume for model caching (shared across runs)
volumes:
  actum-huggingface:
    name: actum-huggingface
    driver: local
